# 数据结构学习助手 - 迭代实施计划

> **版本:** v2.5
> **创建日期:** 2026-06-01
> **目标:** 按照 TODO.md 优先级顺序执行 P1 → P2 → P3 任务

---

## 当前状态

**已完成 (v2.4):**
- ✅ D3 按需导入优化
- ✅ 图页面统一使用 Visualizer 组件
- ✅ Vitest 测试框架配置（42 tests）
- ✅ React.memo 性能优化

---

## P1 - 高优先级任务（核心功能完善）

### 任务 1: 添加缺失的排序算法

**目标:** 覆盖主流排序算法，满足课程演示需求

#### 步骤 1.1: 快速排序 (Quick Sort)
1. 创建 `src/algorithms/sorting/quickSort.js`
2. 实现快速排序逻辑（带分区动画步骤记录）
3. 在 `src/algorithms/sorting/index.js` 中注册
4. 更新 `src/pages/SortPage.jsx` 添加快速排序按钮
5. 验证: `npm run test:run` + 手动预览测试

#### 步骤 1.2: 归并排序 (Merge Sort)
1. 创建 `src/algorithms/sorting/mergeSort.js`
2. 实现归并排序逻辑（带合并动画步骤记录）
3. 注册算法
4. 更新 SortPage
5. 验证

#### 步骤 1.3: 堆排序 (Heap Sort)
1. 创建 `src/algorithms/sorting/heapSort.js`
2. 实现堆排序逻辑（带堆化动画步骤记录）
3. 注册算法
4. 更新 SortPage
5. 验证

**预期工时:** 2-3 天
**验收标准:**
- 3 个新排序算法全部可用
- 动画效果流畅，无卡顿
- 通过 lint 和 build 检查

---

### 任务 2: 扩展单元测试

**目标:** 核心逻辑测试覆盖率 > 70%（当前已有 42 tests，继续扩展）

#### 步骤 2.1: useArrayState 测试
1. 创建 `src/__tests__/useArrayState.test.js`
2. 测试用例:
   - insert: 正常插入、边界插入、最大值限制
   - remove: 正常删除、索引越界
   - search: 查找存在/不存在的元素
   - randomize: 生成随机数组
   - loadData: 加载外部数据
3. 验证: `npm run test:run`

#### 步骤 2.2: dataExport 测试
1. 创建 `src/__tests__/dataExport.test.js`
2. 测试用例:
   - exportData: 导出格式正确
   - importData: 导入验证（版本校验、数据格式）
   - 错误场景处理

#### 步骤 2.3: useStackState / useQueueState 测试
1. 创建 `src/__tests__/useStackState.test.js`
2. 创建 `src/__tests__/useQueueState.test.js`
3. 测试 push/pop/peek/clear 操作

**预期工时:** 2 天
**验收标准:**
- 新增 > 50 个测试用例
- 核心逻辑覆盖率 > 70%
- 所有测试通过

---

### 任务 3: 二叉树操作增强

**目标:** 完善二叉树 CRUD 操作

#### 步骤 3.1: 添加层序遍历 (BFS)
1. 在 `src/hooks/useTreeState.js` 中添加 `levelOrder()` 方法
2. 在 `src/visualizers/treeVisualizer.js` 中添加 `animateLevelOrder()` 动画
3. 更新 `src/pages/TreePage.jsx` 添加层序遍历按钮
4. 测试层序遍历动画

#### 步骤 3.2: 添加查找节点功能
1. 在 `useTreeState.js` 中添加 `search(value)` 方法
2. 在 `treeVisualizer.js` 中添加 `animateSearch()` 动画
3. 更新 TreePage 添加查找输入框和按钮
4. 测试查找功能

#### 步骤 3.3: 添加删除节点功能（考虑子树重平衡）
1. 在 `useTreeState.js` 中添加 `deleteNode(value)` 方法
2. 处理三种情况:
   - 叶子节点删除
   - 单子树节点删除
   - 双子树节点删除（找后继/前驱）
3. 在 `treeVisualizer.js` 中添加 `animateDelete()` 动画
4. 更新 TreePage 添加删除功能
5. 测试各种删除场景

**预期工时:** 2-3 天
**验收标准:**
- 层序遍历、查找、删除功能全部可用
- 动画效果流畅
- 边界情况处理正确

---

### 任务 4: 链表操作增强

**目标:** 丰富链表操作演示

#### 步骤 4.1: 添加按位置插入/删除
1. 在 `src/hooks/useLinkedListState.js` 中添加 `insertAt(index, value)` 和 `deleteAt(index)` 方法
2. 在 `src/visualizers/linkedListVisualizer.js` 中添加对应动画
3. 更新 `src/pages/LinkedListPage.jsx` 添加位置输入和操作按钮
4. 测试各种位置操作

#### 步骤 4.2: 添加链表反转可视化
1. 在 `useLinkedListState.js` 中添加 `reverse()` 方法
2. 在 `linkedListVisualizer.js` 中添加 `animateReverse()` 动画（逐步反转过程）
3. 更新 LinkedListPage 添加反转按钮
4. 测试反转动画

#### 步骤 4.3: 添加环检测可视化
1. 在 `useLinkedListState.js` 中添加 `detectCycle()` 方法（快慢指针）
2. 在 `linkedListVisualizer.js` 中添加 `animateCycleDetection()` 动画
3. 添加创建环的功能（用于演示）
4. 测试环检测

**预期工时:** 2-3 天
**验收标准:**
- 按位置操作、反转、环检测全部可用
- 动画清晰展示操作过程
- 边界情况处理正确

---

## P2 - 中优先级任务（体验优化）

### 任务 5: 性能优化

#### 步骤 5.1: D3 大数据量渲染优化
1. 在 `arrayVisualizer.js` 中实现虚拟化/分块渲染
2. 设置数据量阈值（>50 元素时优化）
3. 测试大数据量场景帧率

#### 步骤 5.2: 图力模拟优化
1. 降低 tick 频率（从默认值降低）
2. 或考虑 Web Worker 迁移（可选）
3. 测试 >20 节点时性能

#### 步骤 5.3: 减少不必要重渲染
1. 为更多组件添加 React.memo
2. 使用 useMemo 优化计算密集型操作
3. 验证优化效果

**预期工时:** 3-4 天
**验收标准:**
- 数组 >50 元素时帧率 > 40fps
- 图 >20 节点时帧率 > 30fps

---

### 任务 6: UI/UX 增强

#### 步骤 6.1: 添加操作撤销预览
1. 在撤销前显示将要回退到的状态
2. 添加确认提示

#### 步骤 6.2: 优化移动端交互体验
1. 测试移动端布局
2. 优化触摸操作
3. 调整响应式断点

#### 步骤 6.3: 添加操作历史时间线
1. 创建时间线组件
2. 显示操作序列和时间戳
3. 支持点击跳转到历史状态

#### 步骤 6.4: 可视化区域网格线密度可配置
1. 添加网格密度设置
2. 支持自定义间距

**预期工时:** 2-3 天
**验收标准:**
- 移动端体验流畅
- 时间线功能可用

---

### 任务 7: 错误处理完善

#### 步骤 7.1: 添加 ErrorBoundary
1. 创建 `src/components/ErrorBoundary.jsx`
2. 包装主要组件
3. 显示友好错误页面

#### 步骤 7.2: 完善动画异常恢复
1. 添加动画失败后的状态恢复
2. 确保 SVG 状态一致

#### 步骤 7.3: 添加网络离线检测
1. 检测网络状态变化
2. 显示离线提示

**预期工时:** 1-2 天
**验收标准:**
- 错误处理完善，无白屏
- 动画异常可恢复

---

## P3 - 低优先级任务（功能扩展）

### 任务 8: 新增数据结构
- [ ] 哈希表（Hash Table）
- [ ] 堆（Heap / Priority Queue）
- [ ] 字典树（Trie）

### 任务 9: 算法对比模式
- [ ] 多算法并行对比执行
- [ ] 算法性能对比图表

### 任务 10: 数据持久化
- [ ] localStorage 存储历史
- [ ] 自定义初始数据配置
- [ ] 分享功能

### 任务 11: 国际化支持
- [ ] 中/英文切换
- [ ] 提取文案到语言文件

**预期工时:** 各 2-7 天
**说明:** 待 P1/P2 完成后再启动

---

## 技术债务清理（与上述任务并行）

| 债务项 | 优先级 | 预计工时 | 关联任务 |
|-------|-------|---------|---------|
| 统一动画引擎调用模式 | P2 | 1 天 | 任务 3, 4 |
| 完善 JSDoc 注释覆盖率 | P3 | 2 天 | 所有任务 |

---

## 里程碑计划

| 里程碑 | 目标版本 | 包含任务 | 预计完成 |
|-------|---------|---------|---------|
| M1: 排序算法扩展 | v2.5 | 任务 1 | 2026-06-05 |
| M2: 测试框架扩展 | v2.6 | 任务 2 | 2026-06-10 |
| M3: 二叉树增强 | v2.7 | 任务 3 | 2026-06-15 |
| M4: 链表增强 | v2.8 | 任务 4 | 2026-06-20 |
| M5: 性能优化 | v2.9 | 任务 5 | 2026-06-30 |
| M6: UI/UX 增强 | v3.0 | 任务 6, 7 | 2026-07-10 |

---

## 验收标准

### 全局标准
- [ ] 所有任务完成后 `npm run build` 通过
- [ ] `npm run lint` 通过，0 errors
- [ ] `npm run test:run` 通过，测试覆盖率 > 70%
- [ ] 所有新增功能手动验证可用
- [ ] 文档同步更新（CHANGELOG.md、TODO.md）

### 质量标准
- 代码符合现有项目规范
- 动画流畅，无卡顿或闪烁
- 错误处理完善，无白屏
- 移动端响应式正常

---

> **说明:** 本计划按照 TODO.md 优先级顺序排列，P1 任务优先执行，P2/P3 依次跟进。
> **最后更新:** 2026-06-01