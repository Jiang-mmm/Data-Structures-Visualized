# v19 M4-1 硬编码中文字符串子清单（P0 4 页面）

> **目标**: 记录 v19 M4-1 子阶段（Home / SortPage / ArrayPage / GraphPage）的实际硬编码中文字符串扫描结果
> **基线**: v19 计划 §3.2 步骤 1.1 + M1 调研清单 §2.1 P0 页面
> **生成时间**: 2026-06-22（深夜）
> **方法**: `Grep` 工具扫描 `[\x{4e00}-\x{9fff}]`（CJK Unified Ideographs 基本平面 + 扩展 A）+ JSX 属性扫描（aria-label / placeholder / data-tooltip / alert / confirm / prompt）
> **状态**: 🟡 步骤 1.1 异常（与 M1 估计偏差 > 95%），等待用户拍板

---

## 1. 异常摘要

| 维度 | 数值 |
|------|------|
| **M1 估计字符数（§2.1 P0 页面）** | ~1,550 字符（Home ~500 + SortPage ~400 + ArrayPage ~350 + GraphPage ~300）|
| **实际扫描硬编码字符数** | **< 50 字符**（仅 7 行注释 + 0 行 JSX 文本 + 0 行 JSX 属性）|
| **差异** | **> 95%**（远超 10% 验收阈值）|
| **根因** | 4 个页面在 v15.x ENH-2 i18n 完善 + v17 UI/UX 迭代过程中**已大量 `t()` 化**，M1 调研时未深入到 v17 实际状态 |
| **步骤 1.1 验收** | ❌ **验收不通过**（差异 > 10% 阈值）|
| **对应 v19 计划异常** | §9.2 异常处理表第 1 项 "字符串数量偏差 > 20%" |
| **处理** | **立即停止 M4-1 步骤 1.2-1.7，等用户拍板下一步**（A/B/C/D 见 §6）|

---

## 2. 4 页面扫描结果

### 2.1 Home.tsx（200 行 / 101 个 `t()` 调用）

| 类别 | 匹配行数 | 字符数 | 字符串 |
|------|----------|--------|--------|
| **JSX 文本节点** | 0 | 0 | — |
| **JSX 属性** | 0 | 0 | — |
| **模板字符串** | 0 | 0 | — |
| **注释** | 6 | ~30 | 见下表 |
| **小计** | **6** | **~30** | — |

**注释匹配**：

| 行号 | 内容 | 类型 | 是否需迁移 |
|------|------|------|------------|
| 19 | `// 4 个辅助区块（ProgressOverview / Recommendations / LearningPath / GlossaryCard）默认折叠，` | 注释 | ❌ 不需（注释）|
| 20 | `// 避免首屏被推至 2 屏外；用户主动点击 "学习中心" 才展开。` | 注释 | ❌ 不需（注释）|
| 24 | `// 线性结构类 (blue)` | 注释 | ❌ 不需 |
| 31 | `// 树结构类 (amber)` | 注释 | ❌ 不需 |
| 40 | `// 图与哈希类 (graph accent)` | 注释 | ❌ 不需 |
| 101 | `{/* v17 R1：学习中心（4 个辅助区块）默认折叠，避免首屏被推至 2 屏外 */}` | JSX 注释 | ❌ 不需（注释）|

**M4 实际范围**：**0 字符**（无用户可见硬编码中文）

**说明**：M1 估计的 "数据结构可视化学习助手"、"学习中心"、"17 种数据结构" 等字符串实际在源文件中**全部已 `t()` 化**（位于行 25-44 / 56-65 / 86-194），无任何 JSX 文本硬编码。

### 2.2 SortPage.tsx（230 行 / 19 个 `t()` 调用）

| 类别 | 匹配行数 | 字符数 | 字符串 |
|------|----------|--------|--------|
| **JSX 文本节点** | 0 | 0 | — |
| **JSX 属性** | 0 | 0 | — |
| **模板字符串** | 0 | 0 | — |
| **注释** | 0 | 0 | — |
| **小计** | **0** | **0** | — |

**M4 实际范围**：**0 字符**（完全 `t()` 化）

**说明**：M1 估计的 "排序算法"、"比较次数"、"交换次数" 等字符串实际全部已 `t()` 化，源文件中无任何中文字符。

### 2.3 ArrayPage.tsx（253 行 / 29 个 `t()` 调用 + 2 个 placeholder `t()`）

| 类别 | 匹配行数 | 字符数 | 字符串 |
|------|----------|--------|--------|
| **JSX 文本节点** | 0 | 0 | — |
| **JSX 属性** | 2 | 0 | `placeholder={t('array.valuePlaceholder')}` / `placeholder={t('array.indexPlaceholder')}`（已 `t()` 化）|
| **模板字符串** | 0 | 0 | — |
| **注释** | 1 | ~10 | `// 数组未排序时 hook 返回 -1 并已提示，不运行动画` |
| **小计** | **3** | **~10** | — |

**M4 实际范围**：**0 字符**（无用户可见硬编码中文）

**说明**：M1 估计的 "数组操作"、"插入元素"、"删除元素"、"按索引访问" 等字符串实际全部已 `t()` 化。

### 2.4 GraphPage.tsx（332 行 / 46 个 `t()` 调用 + 5 个 placeholder `t()`）

| 类别 | 匹配行数 | 字符数 | 字符串 |
|------|----------|--------|--------|
| **JSX 文本节点** | 0 | 0 | — |
| **JSX 属性** | 5 | 0 | `placeholder={t('graph.source')}` / `placeholder={t('graph.target')}` / `placeholder={t('graph.weight')}` × 3（已 `t()` 化）|
| **模板字符串** | 0 | 0 | — |
| **注释** | 0 | 0 | — |
| **小计** | **5** | **0** | — |

**M4 实际范围**：**0 字符**（完全 `t()` 化）

**说明**：M1 估计的 "图操作"、"添加节点"、"添加边"、"深度优先遍历" 等字符串实际全部已 `t()` 化。

---

## 3. 与 M1 调研清单对账

| 页面 | M1 估计字符 | 实际字符 | 差异 | 状态 |
|------|-------------|----------|------|------|
| **Home** | ~500 | **~30**（全为注释）| -94% | 🟢 实际几乎无 UI 硬编码 |
| **SortPage** | ~400 | **0** | -100% | 🟢 完全 `t()` 化 |
| **ArrayPage** | ~350 | **~10**（全为注释）| -97% | 🟢 实际几乎无 UI 硬编码 |
| **GraphPage** | ~300 | **0** | -100% | 🟢 完全 `t()` 化 |
| **总计** | **~1,550** | **< 50** | **> 95% 偏差** | ❌ **M1 估计严重失真** |

---

## 4. 4 页面 `t()` 化进度（实际状态）

| 页面 | 文件行数 | `t()` 调用数 | 命名空间 | 状态 |
|------|----------|--------------|----------|------|
| **Home** | 200 | 101 | `home.*` / `array.*` / `stack.*` / `queue.*` / `tree.*` / `graph.*` 等 | ✅ **完全 `t()` 化**（v17 R1 已完成）|
| **SortPage** | 230 | 19 | `sort.*` | ✅ **完全 `t()` 化** |
| **ArrayPage** | 253 | 29 + 2 placeholder | `array.*` | ✅ **完全 `t()` 化** |
| **GraphPage** | 332 | 46 + 5 placeholder | `graph.*` | ✅ **完全 `t()` 化** |

**关键发现**：M1 调研（2026-06-22 早些时候）时间点为 v19 M0 拍板后立即执行，可能未充分考虑 v15.x ENH-2（i18n glossary 完善）+ v17 UI/UX 迭代（已先期迁移大量 UI 文本）所累积的 `t()` 化进度。

---

## 5. 步骤 1.1 验收对照（v19 计划 §3.2）

| 验收项 | 结果 |
|--------|------|
| 4 个文件全部扫描，零遗漏 | ✅ 通过（Home / SortPage / ArrayPage / GraphPage 全部扫描）|
| 子清单含：字符串原文 / 文件:行号 / 上下文 / 目标 namespace / 建议 key | ✅ 通过（§2 详细列出）|
| 与 M1 总清单的字符数对账（差异 ≤10%）| ❌ **不通过**（实际差异 > 95%）|

**步骤 1.1 验收结论**：❌ **不通过**（需用户拍板下一步）

---

## 6. 后续选项（待用户拍板）

| # | 选项 | 影响 |
|---|------|------|
| **A** | M4-1 实际范围极小（<50 字符注释），直接收尾 | 立即归档 M4-1（无需 17 locale 文件）；M4-2/3 重新评估 |
| **B** | M1 调研严重失真，重新做 M1 调研（覆盖 v17 实际状态）| 推迟 M4 实施；优先做"M1.5 v17 状态重审计"子阶段 |
| **C** | M4-1 步骤 1.2-1.7 调整为"清理剩余注释"（仅 ~30 字符）| M4-1 缩为 0.5d；17 locale 文件**不创建**（M4-2/3 重新评估）|
| **D** | M4-1 视为"补漏 + 验证"，M4-2 重新评估 13 页面 P1 | M4-1 跳过 1.2-1.7；M4-2 重新做硬编码扫描 |

**默认推荐（**）**：**A. 直接收尾 M4-1**（基于"4 页面已完全 `t()` 化"的事实 + M3 规则已能拦截未来硬编码新增）

**关键依据**：
- 4 页面已 100% `t()` 化（实际字符 0）
- M3 阶段已上线 `no-hardcoded-chinese-in-jsx` 规则（warn 级）+ `AssertSameKeys` 编译时断言
- 后续页面（P1/P2）+ 组件（M5）+ utils（M6）才是 M4+ 的真正战场

---

## 7. 范围外（Out of Scope — M4-1 步骤 1.1）

- ❌ 不修改任何代码（步骤 1.1 有边界）
- ❌ 不写测试
- ❌ 不创建 locale 文件
- ❌ 不进入步骤 1.2-1.7（验收不通过，必须先拍板）
- ❌ 不擅自调整 M4 计划（按 §十二"不擅自拍板"原则）

---

## 8. 启动日志

| 时间 | 阶段 | 关键事件 |
|------|------|----------|
| 2026-06-22 23:50 | 基线检查 | 当前分支 feature/v19-i18n-progressive-migration / HEAD 64d4bba / 工作区 clean |
| 2026-06-22 23:51 | 创建分支 | `git checkout -b feature/v19-m4-pages-migration` 成功 |
| 2026-06-22 23:52 | 步骤 1.1 扫描 | 4 页面初次扫描：Home 6 / SortPage 0 / ArrayPage 1 / GraphPage 0 |
| 2026-06-22 23:53 | 步骤 1.1 扩展扫描 | JSX 属性扫描：Home 0 / SortPage 0 / ArrayPage 2 (已 `t()` 化) / GraphPage 5 (已 `t()` 化) |
| 2026-06-22 23:54 | 异常诊断 | 4 页面实际硬编码 < 50 字符，与 M1 估计 ~1,550 差异 > 95% |
| 2026-06-22 23:55 | 步骤 1.1 验收 | ❌ 验收不通过（差异 > 10% 阈值），按 §9.2 异常处理立即停止 |

---

## 9. 关联文档

- v19 主计划：[docs/superpowers/plans/2026-06-22-v19-i18n-progressive-migration.md](../plans/2026-06-22-v19-i18n-progressive-migration.md)
- M4 实施真源：[docs/superpowers/plans/2026-06-22-v19-m4-pages-migration.md](../plans/2026-06-22-v19-m4-pages-migration.md)
- M1 调研清单：[docs/superpowers/i18n-inventory/01-hardcoded-strings-inventory.md](./01-hardcoded-strings-inventory.md)
- v17 UI/UX 迭代：[docs/superpowers/plans/2026-06-22-v17-ui-ux-iterations.md](../plans/2026-06-22-v17-ui-ux-iterations.md)
- v15.x ENH-2：[docs/superpowers/plans/2026-06-22-i18n-glossary-v15-enh2.md](../plans/2026-06-22-i18n-glossary-v15-enh2.md)
