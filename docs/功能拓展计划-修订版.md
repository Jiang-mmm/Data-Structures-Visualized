# 数据结构学习助手3 —— 功能拓展计划（修订版）

> **文档版本：** v2.0（修订版）
> **编制日期：** 2026-06-19
> **目标项目：** `d:\VibeCoding\数据结构学习助手3`
> **参考项目：** `d:\VibeCoding\数据结构学习助手2`
> **修订依据：** 基于对两个项目源码的逐文件审查，修正 v1.0 计划中与当前架构不符的判断
> **核心原则：** 只借鉴功能内容，不借鉴 UI 设计（当前 Neo-Brutalist 风格保持不变）

---

## 1. 功能差异对比表（仅功能，不含 UI）

| 功能维度 | 当前项目（助手3） | 参考项目（助手2） | 差距 |
|----------|------------------|------------------|------|
| **基础数据结构** | 11 种：数组/栈/队列/链表/二叉树/图/哈希/堆/字典树/排序/算法对比 | 11 种基础 + 高级结构 | 缺高级结构 |
| **高级树结构** | 仅 BST（数组索引表示） | AVL、红黑树、B 树 | 全缺 |
| **高级线性/索引结构** | 无 | 跳表、并查集、线段树、树状数组、稀疏表、布隆过滤器、LRU | 全缺 |
| **字符串算法** | 无 | KMP、Manacher、Rabin-Karp | 全缺 |
| **图算法** | BFS/DFS/Dijkstra/拓扑/Bellman-Ford/Floyd/Prim/Kruskal（8 种） | + A*、关键路径 CPM、并查集 | 缺 A*/CPM |
| **排序算法** | 10 种（冒泡/快排/归并/堆/选择/插入/基数/桶/希尔/梳/Tim） | 同等覆盖 | 持平 |
| **全局搜索** | 无（侧边栏手动滚动） | GlobalSearch + 快捷键 | 缺 |
| **统一设置面板** | 仅主题/语言/速度入口 | SettingsPanel 统一面板 | 缺 |
| **新手引导** | 无 | GuideTooltip/GuidedTutorial | 缺 |
| **导出能力** | 数据 JSON/CSV | + SVG→PNG、日志、代码片段 | 部分缺 |
| **测验系统** | 无 | Quiz + 题库 | 缺 |
| **成就系统** | 无 | Achievements + 解锁事件 | 缺 |
| **间隔重复** | 无 | Review（SM-2 算法） | 缺 |
| **笔记/收藏** | 无 | NotesPanel/BookmarkButton | 缺 |
| **应用场景** | ContentTier 有 realWorldApplications 配置 | 专用 ApplicationScenarios 组件 | 部分有 |
| **学习路径力图** | 线性列表 LearningPath | D3 force 力导向图 | 缺力图 |
| **动画录制回放** | 无 | AnimationRecorder | 缺 |
| **步骤单步执行** | 仅播放/暂停 | 上一步/下一步/停止/重置 | 缺 |
| **代码面板** | StepExplainer（临时高亮） | CodePanel（常驻侧边栏） | 部分有 |
| **Web Worker 排序** | 无（主线程） | sortWorker | 缺 |
| **虚拟滚动** | 无（日志上限 100） | virtualScroll | 缺 |
| **rafThrottle** | 无（仅 animationEngine FPS 监控） | animationFrameThrottle | 缺 |
| **骨架屏** | 通用 Spinner | PageSkeleton 按页面类型 | 缺 |
| **生产 console 清理** | 无 | esbuild.drop | 缺 |

---

## 2. 修订后的可借鉴功能清单

> 判断标准：保留（架构直接容纳）/ 调整（需适配）/ 舍弃（冲突或低收益）
> 优先级：P1 高价值快融入 / P2 高价值需改造 / P3 体验增强

### 2.1 数据结构与算法扩展（核心功能借鉴）

| 功能 | 判断 | 技术适配要点 | 优先级 | 估算工时 |
|------|------|-------------|--------|----------|
| **AVL 树** | 调整 | 数据表示改用递归对象 `AvlNode { value, left, right, height }`（参考 [useTrieState.ts](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useTrieState.ts) 的 TrieNode 模式），不可变更新 + `flattenAvl` 转为 `{nodes, edges}` 供可视化器使用；旋转逻辑从 [redBlackTree.js](file:///d:/VibeCoding/数据结构学习助手2/src/algorithms/redBlackTree.js) 的 `_rotateLeft/_rotateRight` 移植并改写为 TS 不可变版本；可视化器复用 [treeVisualizer.ts](file:///d:/VibeCoding/数据结构学习助手3/src/visualizers/treeVisualizer.ts) 的布局思路但适配扁平化数据 | P1 | 4-5 天 |
| **红黑树** | 调整 | 同 AVL，但需额外处理颜色属性；旋转/重染色动画较复杂，建议先实现静态渲染 + 基础插入动画，重染色作为第二阶段；参考 [redBlackTree.js](file:///d:/VibeCoding/数据结构学习助手2/src/algorithms/redBlackTree.js) 的 `_fixInsert` 逻辑 | P1 | 5-6 天 |
| **跳表 SkipList** | 调整 | 数据表示用扁平化 `{ nodes: {value, level}[], edges: {from, to, level}[] }`；从 [skipList.js](file:///d:/VibeCoding/数据结构学习助手2/src/algorithms/skipList.js) 移植 `insert/search/delete` 逻辑并改写为 TS；多层连线可视化需新建 visualizer | P1 | 3-4 天 |
| **并查集 Union-Find** | 调整 | 数据表示用 `{ parent: Record<id, id>, rank: Record<id, number> }`；路径压缩/按秩合并动画；可复用 [graphVisualizer.ts](file:///d:/VibeCoding/数据结构学习助手3/src/visualizers/graphVisualizer.ts) 的节点渲染 | P1 | 3 天 |
| **B 树** | 调整 | 数据表示用递归对象 `BNode { keys: number[], children: BNode[], isLeaf }`；分裂/合并动画复杂，建议先静态渲染 | P2 | 5-7 天 |
| **线段树** | 调整 | 数据表示用数组 `number[]`（与现有 TreeState 一致，索引 2i+1/2i+2）；区间查询/更新/懒标记动画 | P2 | 5-6 天 |
| **树状数组 BIT** | 调整 | 数据表示用 `number[]`；lowbit 路径高亮动画 | P2 | 4-5 天 |
| **稀疏表** | 调整 | 数据表示用二维数组 `number[][]`；RMQ 查询动画 | P2 | 4 天 |
| **布隆过滤器** | 调整 | 数据表示用 `boolean[]` 位数组 + 多哈希函数；插入/查询动画 | P2 | 3-4 天 |
| **LRU Cache** | 调整 | 数据表示用 `{ map: Record<key, value>, order: key[] }`；哈希+双向链表结合动画 | P2 | 3-4 天 |
| **斐波那契堆** | 舍弃 | consolidate 动画过于复杂，教学收益有限，技术债务高；建议作为长期储备，优先级 P3 | P3 | 5-7 天 |
| **KMP/Manacher/Rabin-Karp** | 调整 | 数据表示用 `{ text: string, pattern: string, nextTable: number[] }`；模式串匹配过程可视化；每个 4-5 天 | P2 | 4-5 天/项 |
| **A* 寻路** | 调整 | 复用 [graphVisualizer.ts](file:///d:/VibeCoding/数据结构学习助手3/src/visualizers/graphVisualizer.ts)；新增 `configs/learning/astar.config.ts` | P2 | 4-5 天 |
| **关键路径 CPM** | 调整 | 复用 graphVisualizer；新增 `configs/learning/cpm.config.ts` | P2 | 4-5 天 |

### 2.2 全局交互与教学辅助

| 功能 | 判断 | 技术适配要点 | 优先级 | 估算工时 |
|------|------|-------------|--------|----------|
| **全局搜索 GlobalSearch** | 保留 | 新建 `src/components/GlobalSearch.tsx` + `src/data/searchIndex.ts`；接入 [useGlobalSettings](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useGlobalSettings.ts) 的 `t()`；快捷键 `Ctrl/Cmd+K`；搜索索引从 [Sidebar.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/Sidebar.tsx) 的 `STRUCTURE_KEYS` 生成 | P1 | 1-2 天 |
| **设置面板 SettingsPanel** | 调整 | 扩展 [useGlobalSettings](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useGlobalSettings.ts) 增加字体/代码主题/紧凑模式等状态，**禁止新建第二套 Context**；新建 `SettingsPanel.tsx` 统一入口 | P1 | 2-3 天 |
| **新手引导 GuideTooltip** | 保留 | 基于 [usePageTracker](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/usePageTracker.ts) 判断首次访问；localStorage 持久化引导状态 | P1 | 2 天 |
| **导出增强 ExportMenu** | 调整 | 复用 [dataExport.ts](file:///d:/VibeCoding/数据结构学习助手3/src/utils/dataExport.ts) 风格；SVG→PNG 用 canvas 转换；日志/代码片段导出 | P1 | 2-3 天 |
| **底部导航 BottomNav** | 舍弃 | 当前 [Sidebar.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/Sidebar.tsx) 移动端抽屉已足够；底部 Tab 与 Neo-Brutalist 风格冲突，收益有限 | - | - |

### 2.3 学习闭环增强

| 功能 | 判断 | 技术适配要点 | 优先级 | 估算工时 |
|------|------|-------------|--------|----------|
| **测验系统 Quiz** | 保留 | 新建 `src/data/quizData.ts` + `src/hooks/useQuiz.ts`；接入 [useLearningProgress](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useLearningProgress.ts) 记录 `quizzesCompleted`；题库按 `configs/learning` 分类 | P1 | 3-4 天 |
| **成就系统 Achievements** | 调整 | 与 [usePageTracker](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/usePageTracker.ts)/[useLearningProgress](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useLearningProgress.ts) 事件联动；成就解锁走 [toastStore](file:///d:/VibeCoding/数据结构学习助手3/src/components/toastStore.ts)；**禁止独立 Context** | P2 | 3 天 |
| **复习提醒 Review（SM-2）** | 调整 | 新建 `useReview` Hook；localStorage 存储 ease/interval；与 Quiz 联动 | P2 | 4-5 天 |
| **笔记/收藏** | 保留 | 以页面 ID 为 key 持久化到 localStorage；接入 `useDataStructureState` 基座风格 | P2 | 2-3 天 |
| **应用场景 ApplicationScenarios** | 调整 | 扩展现有 [ContentTier.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/ContentTier.tsx)（已有 realWorldApplications 配置），不新建组件 | P2 | 2 天 |
| **学习路径力图** | 调整 | 复用 [graphVisualizer.ts](file:///d:/VibeCoding/数据结构学习助手3/src/visualizers/graphVisualizer.ts) 力导向能力；数据从 [learningPath.ts](file:///d:/VibeCoding/数据结构学习助手3/src/configs/learningPath.ts) 生成 | P2 | 3-4 天 |

### 2.4 可视化与动画能力

| 功能 | 判断 | 技术适配要点 | 优先级 | 估算工时 |
|------|------|-------------|--------|----------|
| **动画录制回放** | 调整 | 记录 [animationEngine](file:///d:/VibeCoding/数据结构学习助手3/src/utils/animationEngine.ts) 的 step 与时间戳；与 SpeedControl 联动 | P2 | 4-5 天 |
| **步骤单步执行** | 调整 | 将算法执行拆分为 steps 数组；提供上一步/下一步/停止/重置；需重构现有实时动画模式 | P2 | 4 天 |
| **代码面板 CodePanel** | 调整 | 增强 [StepExplainer.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/StepExplainer.tsx) 或新增常驻侧边栏；与 `configs/learning/*.config.ts` 的 `codeSnippet` 集成 | P2 | 2-3 天 |
| **rafThrottle** | 保留 | 新建 `src/utils/animationFrameThrottle.ts`（或合并到 [debounce.ts](file:///d:/VibeCoding/数据结构学习助手3/src/utils/debounce.ts)）；在 [useVisualizer](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useVisualizer.ts) 的 ResizeObserver 回调使用 | P2 | 2 天 |

### 2.5 性能与工程能力

| 功能 | 判断 | 技术适配要点 | 优先级 | 估算工时 |
|------|------|-------------|--------|----------|
| **Web Worker 排序** | 调整 | 新建 `src/workers/sortWorker.ts` + `src/utils/sortWorkerClient.ts`；**Worker 只负责生成 steps**，主线程调用 [animationEngine](file:///d:/VibeCoding/数据结构学习助手3/src/utils/animationEngine.ts) 播放；失败降级主线程 | P2 | 3-4 天 |
| **虚拟滚动** | 调整 | 新建 `src/utils/virtualScroll.ts`；在 [LogPanel.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/LogPanel.tsx)/[Timeline.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/Timeline.tsx) 接入；保持 a11y 焦点管理 | P2 | 2-3 天 |
| **页面骨架屏** | 保留 | 在 [App.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/App.tsx) 的 `PageLoader` 按页面类型分发 | P3 | 1-2 天 |
| **生产 console 清理** | 保留 | 在 `vite.config.js` 增加 `esbuild.drop: ['console','debugger']`（保留 Sentry） | P3 | 0.5 天 |

---

## 3. 分阶段实施路线

### 阶段一：高级数据结构扩展（第 1-6 周）

**目标：** 补齐教学核心缺口，数据结构覆盖提升至 15+ 种

**交付物：**
1. AVL 树（P1，4-5 天）
2. 跳表 SkipList（P1，3-4 天）
3. 并查集 Union-Find（P1，3 天）
4. 红黑树（P1，5-6 天）
5. 全局搜索 GlobalSearch（P1，1-2 天）

**验收标准：**
- 每个数据结构遵循 8 步流程：Hook → Visualizer → Page → 路由 → 导航 → 首页卡片 → 测试 → 学习配置
- 所有新增代码 100% TypeScript，UI 文案走 `t()`
- `npm run lint && npm run typecheck && npm run test:run && npm run build` 全通过
- Bundle 预算保持：index < 80KB、vendor-react < 250KB、vendor-d3 < 60KB

**依赖关系：**
- AVL 树是后续红黑树/B 树的模式基础，必须先完成
- 全局搜索依赖 [Sidebar.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/Sidebar.tsx) 的 `STRUCTURE_KEYS`，可并行开发

### 阶段二：学习闭环与教学辅助（第 7-12 周）

**目标：** 形成"学-练-测-复习"闭环

**交付物：**
1. 测验系统 Quiz（P1，3-4 天）
2. 设置面板 SettingsPanel（P1，2-3 天）
3. 新手引导 GuideTooltip（P1，2 天）
4. 导出增强 ExportMenu（P1，2-3 天）
5. 成就系统 Achievements（P2，3 天）
6. 应用场景扩展（P2，2 天）

**验收标准：**
- 学习进度可展示"已完成测验/成就"维度
- [useLearningProgress](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useLearningProgress.ts) 同步机制正常
- 新增 `src/data/quizData.ts`、`src/data/achievements.ts` 等数据文件

### 阶段三：高级结构与算法补充（第 13-18 周）

**交付物：**
1. B 树（P2，5-7 天）
2. 线段树（P2，5-6 天）
3. 树状数组 BIT（P2，4-5 天）
4. 稀疏表（P2，4 天）
5. 布隆过滤器（P2，3-4 天）
6. LRU Cache（P2，3-4 天）
7. KMP/Manacher/Rabin-Karp（P2，4-5 天/项）
8. A*/CPM 图算法（P2，4-5 天/项）

### 阶段四：性能与工程加固（第 19-22 周）

**交付物：**
1. Web Worker 排序（P2，3-4 天）
2. rafThrottle 接入（P2，2 天）
3. 虚拟滚动 LogPanel/Timeline（P2，2-3 天）
4. 动画录制回放（P2，4-5 天）
5. 步骤单步执行（P2，4 天）
6. 生产 console 清理 + Bundle 复核（P3，1 天）

---

## 4. 关键技术适配要点（引用当前项目实际文件）

### 4.1 数据表示模式选择

当前项目有三种数据表示模式，新增数据结构应按类型选择：

| 数据结构类型 | 表示模式 | 参考文件 | 适用场景 |
|-------------|---------|---------|---------|
| 线性结构 | `number[]` | [useArrayState.ts](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useArrayState.ts) | 数组/栈/队列/堆/排序 |
| 完全二叉树 | `number[]`（索引 2i+1/2i+2） | [useTreeState.ts](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useTreeState.ts) | BST/线段树/树状数组 |
| 递归结构 | 对象引用 + 不可变更新 | [useTrieState.ts](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useTrieState.ts) | Trie/AVL/红黑树/B 树 |
| 图结构 | `{ nodes, links }` | [useGraphState.ts](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useGraphState.ts) | 图/并查集/A*/CPM |
| 扁平化结构 | `{ nodes, edges }` | Trie 的 `flattenTrie` 函数 | 跳表/稀疏表 |

**AVL/红黑树关键决策：** 必须采用递归对象表示（参考 TrieNode），因为旋转操作在数组索引表示下极其复杂且易错。

### 4.2 状态基座接入规范

所有新增数据结构必须继承 [useDataStructureState](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useDataStructureState.ts)：

```typescript
// 标准接入模式（参考 useTrieState）
export function useAvlTreeState() {
  const {
    data, logs, isAnimating, setIsAnimating,
    push, addLog, reset, loadData,
    undo, redo, canUndo, canRedo, getUndoPreview, getRedoPreview,
  } = useDataStructureState<AvlNode>(buildInitialTree(), { storageKey: 'avl-tree' })

  // 操作函数：不可变更新 + push 新状态
  const insert = useCallback((value: number) => {
    const newData = insertAvl(data, value)  // 不可变更新
    push(newData)
    addLog('oper', tStatic('hooks.avlLogInsert').replace('{value}', String(value)))
    showToast({ type: 'success', message: tStatic('hooks.avlInsertSuccess') })
  }, [data, push, addLog])

  return { data, logs, isAnimating, setIsAnimating, insert, /* ... */ }
}
```

**硬性约束：**
- 禁止引入第二套状态管理体系（禁止独立 Context）
- 禁止使用 `useUndoRedo`（参考项目的双历史栈会冲突）
- 所有操作必须不可变更新（`push` 新对象，不修改原对象）

### 4.3 可视化器规范

新增可视化器必须遵循 [treeVisualizer.ts](file:///d:/VibeCoding/数据结构学习助手3/src/visualizers/treeVisualizer.ts) 模式：

```typescript
// 必须导入
import { select } from '../utils/d3Imports'                    // 单一 D3 入口
import { duration, EASING, transitionEnd, measureRender, type Animation } from '../utils/animationEngine'
import { getColors, detectDarkMode, ensureGradientDefs, gradUrl } from '../utils/themeColors'
import { getLargeDataThreshold } from '../utils/performanceConfig'
import { tStatic } from '../i18n/useI18n'

// 渲染函数：全清 + 全绘
export function renderAvlTree(svg: SVGSVGElement, data: AvlNode, options: AvlTreeOptions) {
  return measureRender('renderAvlTree', () => {
    const container = select(svg)
    container.selectAll('*').interrupt()
    container.selectAll('*').remove()                          // 全清
    ensureGradientDefs(svg, isDark)
    // ... 全绘逻辑
  })
}

// 动画函数：检查 abort + 大数据跳过
export async function animateInsertAvl(svg, value, data, options, anim?: Animation) {
  if (nodeCount > getLargeDataThreshold('tree')) return        // 大数据跳过
  // ... 每步检查
  if (anim?.isAborted?.()) return
}
```

### 4.4 路由/导航/首页接入

新增页面必须完成三处接入：

1. **路由：** [App.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/App.tsx) 添加 `React.lazy` 导入 + `<Route>`
2. **导航：** [Sidebar.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/Sidebar.tsx) 的 `STRUCTURE_KEYS` 数组添加条目 + `getIconSvg` 添加图标
3. **首页：** [Home.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/pages/Home.tsx) 的 `structures` 数组添加卡片

### 4.5 学习配置接入

新增学习配置必须：
1. 在 `src/configs/learning/` 新建 `xxx.config.ts`，导出 `xxxConfig: LearningModeConfig`
2. 在 [configs/learning/index.ts](file:///d:/VibeCoding/数据结构学习助手3/src/configs/learning/index.ts) 注册
3. 类型遵循 [types/learning.d.ts](file:///d:/VibeCoding/数据结构学习助手3/src/types/learning.d.ts) 的 `LearningModeConfig`

### 4.6 i18n 文案接入

所有新增 UI 文案必须走 [i18n/useI18n.ts](file:///d:/VibeCoding/数据结构学习助手3/src/i18n/useI18n.ts) 的 `t()`（组件内）或 `tStatic()`（非组件内）：
- 中文文案必填
- 英文文案可后续补全
- 文案 key 遵循 `模块.功能` 命名（如 `avl.title`、`hooks.avlLogInsert`）

---

## 5. 风险识别与应对

| 风险 | 等级 | 应对策略 |
|------|------|----------|
| AVL/红黑树旋转逻辑在数组索引表示下易错 | 高 | 采用递归对象表示（参考 TrieNode），不可变更新；旋转逻辑从参考项目移植并改写为 TS |
| 新增数据结构动画复杂（红黑树重染色/B 树分裂） | 高 | 先实现核心操作与静态渲染，动画作为第二阶段；复用参考项目的旋转/分裂逻辑 |
| Bundle 超预算（index < 80KB） | 中 | 新增页面必须 `React.lazy`；高级结构按页面拆 chunk；定期跑 `npm run build:analyze` |
| 学习辅助功能导致 Context 膨胀 | 中 | 统一收敛到 [useGlobalSettings](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useGlobalSettings.ts) 或轻量 Store，禁止每个功能独立 Context |
| Web Worker 与现有排序接口不兼容 | 中 | Worker 只负责生成 steps，主线程保留 [animationEngine](file:///d:/VibeCoding/数据结构学习助手3/src/utils/animationEngine.ts) 播放控制 |
| JS→TS 迁移引入类型错误 | 中 | 分文件迁移，每文件补齐类型后立即跑 `npm run typecheck` |
| 无障碍（a11y）回归 | 中 | 每阶段跑 `node e2e/test-a11y.js`，新增组件必须补充 `aria-label`/`role` |
| 文档与测试滞后 | 高 | 严格执行"新增必测试、变更必文档"，每阶段更新本计划与 `PROJECT_SUMMARY.md` |

---

## 6. 最小启动集建议

若资源有限，建议按以下顺序启动（约 6-8 周可显著提升产品竞争力）：

### 第 1 周：AVL 树（首个高级数据结构）
- 文件：`src/types/avl.d.ts`、`src/algorithms/avlTree.ts`、`src/hooks/useAvlTreeState.ts`、`src/visualizers/avlTreeVisualizer.ts`、`src/pages/AvlTreePage.tsx`
- 验证：遵循 8 步新增流程，单元测试覆盖旋转逻辑（LL/RR/LR/RL 四种情况）

### 第 2 周：跳表 + 并查集
- 文件：`src/algorithms/skipList.ts`、`src/algorithms/unionFind.ts` + 对应 Hook/Visualizer/Page
- 验证：多层连线可视化、路径压缩动画

### 第 3 周：全局搜索 + 测验系统试点
- 文件：`src/components/GlobalSearch.tsx`、`src/data/searchIndex.ts`、`src/components/Quiz.tsx`、`src/data/quizData.ts`
- 验证：快捷键 `Ctrl/Cmd+K`、答题后学习进度同步

### 第 4-5 周：红黑树 + 设置面板
- 文件：`src/algorithms/redBlackTree.ts` + 对应 Hook/Visualizer/Page、`src/components/SettingsPanel.tsx`
- 验证：旋转/重染色动画、设置持久化

### 第 6 周：Web Worker 排序 + 回归测试
- 文件：`src/workers/sortWorker.ts`、`src/utils/sortWorkerClient.ts`
- 验证：100 元素排序主线程不卡顿、全量 E2E 回归

完成上述 6 项后，即可评估是否继续推进完整路线图。

---

## 7. v1.0 计划修正说明

| v1.0 判断 | 修正后判断 | 修正原因 |
|-----------|-----------|---------|
| 底部导航 BottomNav（P2） | 舍弃 | 与 Neo-Brutalist 风格冲突，当前移动端抽屉已足够 |
| 斐波那契堆（P3） | 舍弃 | consolidate 动画过于复杂，教学收益有限 |
| AVL 用数组索引表示 | 调整为递归对象表示 | 旋转操作在数组索引表示下极其复杂，参考 TrieNode 模式更清晰 |
| 应用场景新建组件 | 调整为扩展现有 ContentTier | 当前 ContentTier 已有 realWorldApplications 配置，无需新建 |
| 学习路径力图复用 graphVisualizer | 确认可行 | 已验证 graphVisualizer 支持力导向布局 |
| 设置面板新建 Context | 调整为扩展现有 useGlobalSettings | 硬性约束禁止第二套 Context |

---

> 本计划为动态文档，建议在每次迭代后更新完成状态、实际工时与风险记录。

---

## 8. 迭代记录

### 8.1 AVL 树（2026-06-19 完成）

**状态：** ✅ 已完成并通过全量验证

**交付物清单（8 步流程）：**
1. ✅ 类型定义：[hooks.d.ts](file:///d:/VibeCoding/数据结构学习助手3/src/types/hooks.d.ts) 新增 `AvlNode`/`AvlFlattened`/`AvlTreeState` 等接口
2. ✅ 算法实现：[avlTree.ts](file:///d:/VibeCoding/数据结构学习助手3/src/algorithms/avlTree.ts) — 递归对象 + 不可变更新，含四种旋转、插入/删除/查找/遍历/扁平化/验证
3. ✅ 核心 Hook：[useAvlTreeState.ts](file:///d:/VibeCoding/数据结构学习助手3/src/hooks/useAvlTreeState.ts) — 继承 `useDataStructureState<AvlNode | null>`
4. ✅ 可视化器：[avlTreeVisualizer.ts](file:///d:/VibeCoding/数据结构学习助手3/src/visualizers/avlTreeVisualizer.ts) — D3 全清+全绘，显示值+平衡因子
5. ✅ 页面组件：[AvlTreePage.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/pages/AvlTreePage.tsx) — `useMemo` 优化扁平化数据
6. ✅ 路由与导航：[App.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/App.tsx)（lazy 路由）、[Sidebar.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/Sidebar.tsx)（图标 case 6）、[Home.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/pages/Home.tsx)（卡片）、[ContentTier.tsx](file:///d:/VibeCoding/数据结构学习助手3/src/components/ContentTier.tsx)、[learningPath.ts](file:///d:/VibeCoding/数据结构学习助手3/src/configs/learningPath.ts)
7. ✅ 学习配置：[avlTree.config.ts](file:///d:/VibeCoding/数据结构学习助手3/src/configs/learning/avlTree.config.ts) — 7 个学习步骤
8. ✅ i18n 文案：[locales.ts](file:///d:/VibeCoding/数据结构学习助手3/src/i18n/locales.ts) — 中英文双语
9. ✅ 单元测试：[avlTree.test.ts](file:///d:/VibeCoding/数据结构学习助手3/src/__tests__/avlTree.test.ts) — 46 个测试（含压力测试）

**验证结果：**
- Lint: 0 errors, 0 warnings
- Typecheck: 0 errors
- Tests: 3042 passed (188 files)
- Build: success, bundle budgets passed（index 55KB < 110KB, vendor-react 230KB < 250KB, vendor-d3 52KB < 60KB）

**遇到的问题与修复：**

| 问题 | 原因 | 修复方案 |
|------|------|---------|
| TypeScript 报错 `AvlFlattenedNode` 未使用 | 可视化器 import 了未使用的类型 | 移除未使用 import |
| AVL 树平衡失效（50 节点高度 33，应≈6） | `rotateRight`/`rotateLeft` 中 `updateHeight(updateHeight(newX))` 调用顺序错误，未先更新下层节点高度就计算上层 | 改为先 `updateHeight` 下层（newY/newX），再用更新后的对象创建上层并 `updateHeight` |
| 6 个测试文件失败（36→17 个测试） | 新增 AVL 学习路径节点导致总模块数 10→11，百分比和统计数期望值需同步 | 更新 `useLearningProgress.test.ts`、`ProgressOverview.test.tsx`、`LearningPath.test.tsx`、`LearningRecommendations.test.tsx`、`learningRecommender.test.ts`、`newLearningConfigs.test.ts` 的期望值 |
| Build 失败 index chunk 116.8KB > 110KB | 新增 AVL i18n 文案导致 index chunk 超预算 | 在 [vite.config.js](file:///d:/VibeCoding/数据结构学习助手3/vite.config.js) `manualChunks` 中将 `src/i18n/locales` 拆分到独立 `i18n-locales` chunk，index 降至 55KB |

**关键技术决策：**
- 数据表示采用递归对象 `AvlNode { value, left, right, height }`（参考 TrieNode），非数组索引表示，因旋转操作在数组索引下极其复杂
- Visualizer 接收扁平化数据 `AvlFlattened { nodes, edges }`（通过 `useMemo(() => getFlattened(), [getFlattened])` 计算），而非原始递归对象
- Sidebar 图标在 case 5（Tree）和 case 6（原 Graph）之间插入 AVL 树后，后续 case 索引全部 +1
